apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: api
spec:
  ports:
    # Service port
  - port: 80
    protocol: TCP
    # Container port
    targetPort: ${CONTAINER_PORT}
  selector:
    app: api
  type: ClusterIP
